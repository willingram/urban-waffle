# Zero to IAA in Sixty Minutes

Steps:

- Enable WSL
- Install Ubuntu from Windows Store
- Install Hyper terminal
- Optional: Install ZSH
- Install nvm
- `nvm use node`
- `npm install -g @aws-amplify/cli`
- `amplify configure` to set up admin account details
